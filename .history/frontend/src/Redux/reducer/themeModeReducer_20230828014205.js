


export const modeReducer = (state = (toggleActive : true), action) => {
    switch(action.type) {
        return {
            ...state,
            toggleActive: state.toggleActive,
            mode : 
        }
    }
}